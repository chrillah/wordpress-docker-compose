<?php get_header(); ?>
<main>
    <section>
        <div class="container">
            <div class=row>
                <div class="col-xs-12 col-md-9">
                    <!-- till home.php en blogg kräver single_post_title() -->
                    <h1>
                        <?php single_post_title(); ?>
                    </h1>
                </div>
            </div>
            <div class="row">
                <?php if (have_posts()):
                    while (have_posts()):
                        the_post(); ?>
                        <!-- INNUTI LOOPEN -->
                        <div class="col-xs-12 xol-md-9">
                            <article>
                                <img src="<?= the_post_thumbnail_url("large") ?>" alt="">
                                <a href="<?= the_permalink(); ?>">
                                    <h1 class="title">
                                        <h1>
                                            <?= the_title(); ?>/h1>
                                </a>
                            </article>
                        </div>
                    <?php endwhile; ?>
                <?php endif; ?>
            </div>
        </div>
    </section>
</main>
<?php get_footer(); ?>
